// View the diagram file here https://app.diagrams.net/
// Remember that include pastes the contents of the file.
// So this will just insert the two declaration as text, replacing the #include.
#include "f.h"

int main()
{
    f(1);
    b(2.0f);
    return 0;
}
